spring:
  cloud:
    gcp:
      project-id: ${GCP-PROJECT-ID:jswone-dev}
      pubsub:
        orchestrator-to-notification: ${NOTIFICATION-PUBSUB-TOPIC:notification-request-queue}

temporal:
  server-base-url: ${TEMPORAL-BASE-URL:34.93.59.150:7233}
  namespace: ${TEMPORAL-NAMESPACE:test}
  invoice-rejection-task-queue: ${INVOICE-REJECTION-QUEUE:inv-rejection-task-queue}

external-service:
  joms:
    base-url: ${JOMS-BASE-URL:https://qa-oms.msme.jswone.in/joms/api}
    api-key:  ${JOMS-API-KEY:3f9a2b5f-7bd8-4a51-b11f-47b30decd925}

notification:
  report-to-email: ${REPORT-TO-EMAIL:nitish.rohilla@jsw.in,v_pratiksha.shete@jsw.in}
  report-cc-email: ${REPORT-CC-EMAIL:nitish.rohilla@jsw.in}
  due-threshold: ${LEDGER_NOTIFICATION_PAYMENT_THRESOLD_VALUE}

